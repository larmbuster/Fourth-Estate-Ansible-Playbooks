---
# MS SQL Server STIG Collections Requirements
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  - name: community.general
    version: ">=8.0.0"

  - name: ansible.windows
    version: ">=2.0.0"

  - name: community.windows
    version: ">=2.0.0"
