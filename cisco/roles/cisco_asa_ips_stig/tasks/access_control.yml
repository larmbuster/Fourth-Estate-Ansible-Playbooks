---
# =========================================
# Cisco ASA IPS STIG - Access Control
# 1 rules
# =========================================

# -----------------------------------------
# CASA-IP-000180 - MEDIUM
# -----------------------------------------

- name: "CASA-IP-000180 | MANUAL CHECK - The Cisco ASA must be configured to block outbound traffic containing denial-of-service (DoS) attacks by ensuring an intrusion prevention policy has been applied to outbound communications traffic."
  ansible.builtin.debug:
    msg: "MANUAL CHECK: Configure access control rules to block noncompliant traffic. Step 1: Navigate to Configuration >> ASA FirePOWER Configuration >> Policies >> Access Control Policy. The Access Control Policy page a..."
  tags:
    - CASA-IP-000180
    - cat2
    - access_control

